import { describe, it } from "vitest";

describe.skip("live chat @live", () => {
  it("sends prompt via broker HTTP", async () => {
    // Run manually: pnpm dev:broker && pnpm test:integration
  });
});
